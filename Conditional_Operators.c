// Conditional Operators:- 
// (condition) ? exp3(true) : exp4(false); 
#include<stdio.h>
int main()
{
    // int a = 50,b= 30;  
    // int c = (a!=b) ? a : b;
    // printf("Greatest value = %d",c);
    int a = 20, b =60;
    int c = (a>=b) ? a : b;
    printf("Greatest value = %d",c);
}